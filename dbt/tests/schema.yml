version:2

models:
  - name: dim_patients
    columns:
      - name:patient_id
        tests:
          - unique
          - not_null
  - name: fct_encounters
    columns:
      - name:encounter_id
        tests:
          - unique
          - not_null
      - name:patient_id
        tests:
          - not_null

