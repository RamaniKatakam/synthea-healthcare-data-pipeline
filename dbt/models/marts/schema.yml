version: 2

models:
  - name: dim_patients
    columns:
      - name: patient_id
        tests:
          - not_null
          - unique

  - name: fct_encounters
    columns:
      - name: encounter_id
        tests:
          - not_null
          - unique
      - name: patient_id
        tests:
          - not_null